{
  "name": "tag-app-custom-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.5",
  "scripts": {
    "dev:ext": "cd apps/extension && bun dev",
    "dev:server": "cd apps/server && bun dev",
    "dev:web": "cd apps/web && bun dev",
    "build:ext": "cd apps/extension && bun run build && cd -",
    "build:server": "cd apps/server && bun run build && cd -",
    "build:web": "cd apps/web && bun run build && cd -",
    "build:cli": "cd apps/cli && bun run build && cd -",
    "build:caddy": "cp apps/Caddyfile apps/server/dist",
    "build:serverFull": "rm -rf apps/server/dist && bun build:server && bun build:cli && bun build:web && bun build:caddy",
    "type:check": "bun run -r type:check",
    "update:all": "bun -r update"
  },
  "devDependencies": {
    "@types/node": "^24.13.3"
  }
}
